# Resto QRCode Frontend (Next)

This is a minimal React + Vite + TypeScript + Tailwind skeleton intended as a migration target.

- No business logic implemented.
- Keep legacy runtime and feature flags in main repo.
